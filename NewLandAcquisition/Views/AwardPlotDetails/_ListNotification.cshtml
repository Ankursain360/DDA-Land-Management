@*@model IEnumerable<Libraries.Model.Entity.Newlandus4plot>*@
@model Libraries.Repository.Common.PagedResult<Libraries.Model.Entity.Newlandawardplotdetails>
<div class="table-section bg-white border shadow rounded ">
    <div class="table-responsive">
        <table class="table table-sm table-bordered mb-0 table-striped">
            <thead class="text-nowrap">
                <tr>
                    <th>Award</th>
                    <th>Village Name</th>
                    <th>Khasra No.</th>
                    <th>Actual Area(Bigha-Biswa-Biswanshi)</th>
                    <th>Used Area(Bigha-Biswa-Biswanshi)</th>

                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Results.Count() > 0)
                {
                    @foreach (var item in Model.Results)
                    {
                <tr>
                    <td>@(item.NewlandAwardMaster==null?"": item.NewlandAwardMaster.AwardNumber)</td>
                    <td>@(item.NewlandVillage==null?"": item.NewlandVillage.Name)</td>
                    <td>@(item.NewlandKhasra == null?"": item.NewlandKhasra.Name)</td>
                    <td>
                        @item.NewlandKhasra.Bigha
                        -
                        @item.NewlandKhasra.Biswa-
                        @item.NewlandKhasra.Biswanshi
                    </td>
                    <td>
                        @item.Bigha
                        -
                        @item.Biswa-
                        @item.Biswanshi
                    </td>


                </tr>
                    }
                }
                else
                {
                    <tr>
                        <th colspan="14" style="text-align:center"> No Record Available! </th>
                    </tr>
                }
            </tbody>
        </table>
        @await Component.InvokeAsync("Pager", new { totalCount = Model.RowCount, pageNumber = Model.CurrentPage, pageSize = Model.PazeSize })
    </div>
</div>
<partial name="../Shared/_ValidationScriptsPartial.cshtml" />
