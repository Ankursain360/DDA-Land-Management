@model IEnumerable<Dto.Search.KycFormApprovalDetailsSearchDto>
@{int rowNo = 0; }
<table class="table table-sm table-bordered mb-0 table-striped">
    <thead class="text-nowrap">
        <tr>
            <th>Sr.No</th>
            <th> Property </th>
            <th>FileNo</th>
            <th>LeaseGroundRentDepositFrequency </th>
            <th> PlotNo </th>
            <th> Phase </th>
            <th> Sector </th>
            <th>EmailId</th>
            <th>MobileNo</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count() > 0)
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>@(rowNo += 1)</td>
                    <td>@item.Property</td>
                    <td>@item.FileNo</td>
                    <td>@item.LeaseGroundRentDepositFrequency</td>
                    <td>@item.PlotNo</td>
                    <td>@item.Phase</td>
                    <td>@item.Sector</td>
                    <td>@item.EmailId</td>
                    <td>@item.MobileNo</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <th colspan="14" style="text-align:center"> No Record Available! </th>
            </tr>
        }
    </tbody>

</table>
