@model Libraries.Model.Entity.Leaseapplication 
@await Component.InvokeAsync("PageHeader", new { pageName = "Allotment Letter" })

<div class="pt-3">
    <form asp-action="Create" method="post" class="d-block rounded border p-3 bg-white search-filer shadow mb-3">
        <h3 class="mb-3 d-flex">Add Details</h3>
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="row mt-md-3">
            <!--<div class="col-md-6">
        <div class="floating-label-wrap">-->
            @*<select class="floating-label-field floating-label-field--s3"
                asp-for="RefNo" asp-items="@(new SelectList(Model.RefNoList, "Id","RefNo"))">
            <option disabled="disabled" selected value="">Select</option>
        </select>*@
            <!--<span class="text-danger"></span>
            <label class="floating-label">Application Number</label>
        </div>
    </div>-->
            <div class="col-md-6">
                <div class="floating-label-wrap">
                    <input placeholder="Name" type="text"
                           class="floating-label-field floating-label-field--s3 " id="txtRefernceNumber" required />
                    <span class="text-danger"></span>
                    <label class="floating-label">Refernce Number   </label>
                </div>
            </div>
            <div class="col-md-6">
                <div class="floating-label-wrap mb-3 mb-md-0">
                    <input placeholder="Name" type="date"
                           class="floating-label-field floating-label-field--s3 " id="txtDate" required/>
                    <span class="text-danger"></span>
                    <label class="floating-label">Date   </label>
                </div>
            </div>
        </div>
        <div class="row mt-md-3">
            
        </div>

        <div class="row mt-md-3">

            <div class=" col-md-12">
                <div class="col-6 col-md-5 col-xl-6">

                </div>
                <button class="btn btn-theme" id="btnReset" type="reset"> Clear </button>
                <button class="btn btn-theme" id="btnGenerate" type="submit">Generate</button>
                @Html.ActionLink("Back", "Index", "AllotmentLetter", null, new { @class = "btn btn-theme " })
            </div>

        </div>
    </form>
</div>



<partial name="../Shared/_ValidationScriptsPartial.cshtml" />
<environment include="Development">
    <script src="~/js/allotmentletterindex.js"></script>
</environment>
<environment exclude="Development">
    <script src="~/js/allotmentletterindex.min.js" asp-append-version="true"></script>
</environment>
